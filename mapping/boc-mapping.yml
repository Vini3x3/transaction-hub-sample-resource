import:
  account:
    BOC:
      mapper-config:
        date-header: Date
        description-header: Transaction Details
        withdrawal-header: Withdrawal
        deposit-header: Deposit
        balance-header: Balance in Original Currency
        account: BOC
        date-pattern: yyyy/MM/dd
      system-tagger-config:
        SCHEDULE:
          GAS:
            - "HONG KONG & CHINA GAS LTD"
          CHARITY:
            - "PO LEUNG KUK"
        TRANSFER:
          FROM-HSBC:
            - "MR CHAN TAI MAN"
